* run through columns -> check if source & column exist
* check if column has correct data
* ?? data granularity level -IMPORTANT !!!
* format (output) shape in PBI
* PBI filters set -> which are required
* embedding ?
* RLS ?
* which fields are mandatory ?
* which fields are optional ? (can be added/removed from the table)
* 



* remove "supplier code"
* decode all (port, airport, carrier etc) names and put into human readable format
* check from Aidens old view - if decoding exists then pick up the logic, if decoding mechanism is needed - ask Aiden
 -> CARRIER NAME (instead of carrier code)
 -> PORT name (AIR & SEA)

* need to count all 20ft container (any type but 20ft)
* fix and check total number of conts (and 20ft count, 40ft count & teus)
* format all dates: 17-Nov-25

* for PORT names use table -> public.analytical__air_sea_ports_codes

* union all PO without freight (see Aiden SQL sample)
* fix 20 & 40 & TEU count
* Status add

-----
* add to commodity quantity (but units are unknown, just keep numbers)
* ETD / ETA - first ETA/ETD
* revised -> wakeo (if missing -> as ETA/ETD)


