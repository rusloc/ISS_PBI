# Daily — 2026-08-19

**Day 10** — derived: origin 2026-08-10 (first commit `9ac9f79`); (2026-08-19 − 2026-08-10) + 1 = 10.
**Reports onboarded: 2** — derived by counting active rows in the CLAUDE.md registry.

_Closed retroactively on 2026-08-20._

## Shipped

**1. ARRIVAL / DEPARTURE date logic committed — `d20fcc3`, 16:40.**
Titled *"change ARRIVAL / DEPARTURE date logic as per Mikhail request"*. Thirteen report files,
roughly 900 lines net:

| Area | File | Lines |
|---|---|---|
| SQL | `context/datasources/sql/COMS PO VIEW.sql` | 680 |
| SQL | `context/datasources/sql/COMS EK VIEW.sql` | 187 |
| Model | `_PO_VIEW_.tmdl` / `_EK_VIEW_.tmdl` | 62 / 47 |
| Model | `cultures/en-US.tmdl` | 268 |
| Model | `EK_VIEW_PARAM.tmdl` / `PO_VIEW_PARAM.tmdl` | 8 / 10 |
| Report | 3 × `visual.json` (page `ea84587d0043583b839d`) | 36 / 38 / 14 |

**These edits were not authored during the 08-19 session** — they were already sitting uncommitted
in the working tree when it opened, carried across the 08-15 → 08-18 dull stretch. What happened on
08-19 was the *delivery*: they went into history. The content of the change is not documented in
this log; it originates from a request by Mikhail and would need reconstructing from the diff.

**2. Log housekeeping.** The 08-15 → 08-18 gap backfilled (twelve dull-day files), today's files
opened, and the three dailies still reading `_Open note. Day not closed._` (2026-08-11, 2026-08-12,
2026-08-14) closed retroactively. Detail in [`plan/2026-08-19.md`](../plan/2026-08-19.md) and
[`coms 2026-08-19.md`](../coms/coms%202026-08-19.md).

## Blocked / open

- **`d20fcc3` is a mixed commit with a misleading message.** It carries sixteen `.log/` files and
  `PO EK view column logic align.txt` under a message that mentions only the ARRIVAL / DEPARTURE
  logic change. `CLAUDE.md` asks for one report — or one batch theme — per commit; this is three
  themes in one. Not fixable in history now, but worth not repeating.
- **The ARRIVAL / DEPARTURE change is undocumented.** No plan entry, no coms entry, no note of what
  Mikhail asked for or which columns moved. The largest delivery in the repo so far has the thinnest
  record. Reconstructing it from the `d20fcc3` diff is an open task.
- **Did the six 08-12 columns ride along?** `COMS EK VIEW.sql` changed by 187 lines in `d20fcc3`,
  and the standing open item is that those six columns were never applied to `public.sql_source`.
  Whether this commit overlaps, supersedes or conflicts with them is unverified.
- **`board.md` is eight days stale** — still headed `# Today — 2026-08-11 (day 2)`.
- Carried unchanged from [2026-08-14](2026-08-14.md): the PO↔EK divergence in `_origin_port_name`;
  the AVS discharge join keying a code column to a name column; `shipment-tracking.md` §8 listing
  three of the five port tables, with no per-table schema doc for any of them.
- Carried from [2026-08-12](2026-08-12.md): the asymmetric `--$sql$` toggle at
  `COMS EK VIEW.sql:19`; `validate.py` blind to BOMs (`utf-8-sig`); board section A undone by the
  report replacement; `_rdd_eta` vs `_nbd_2_eta` duplication; no house theme; ops unknowns.

## Next

Reconstruct what `d20fcc3` actually changed and write it up, then check it against the six
outstanding EK columns from 08-12 before anything is applied to `public.sql_source`.
