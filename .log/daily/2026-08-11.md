# Daily — 2026-08-11

**Day 2** — derived: origin 2026-08-10 (first commit `9ac9f79`); (2026-08-11 − 2026-08-10) + 1 = 2.
**Reports onboarded: 2** — derived by counting active rows in the CLAUDE.md registry.

_Closed retroactively on 2026-08-19 — see [Closed](#closed) at the end._

## Shipped so far

- 2026-08-10 closed properly: full EOD appended to `.log/daily/2026-08-10.md` (bootstrap, both
  reports audited, shipment-tracking domain captured, five commits listed).
- Today's coms file opened; plan drafted on the board.
- **Correction landed**: `__FX__` was documented yesterday as "FX rates / currency conversion" —
  wrong, inferred from the table name. It is a measures table of `FIRSTNONBLANK()` column wrappers
  (`FX` = fix), 7 measures + a `Placeholder` column. Fixed in `context/reports/coms.md` and
  `context/business-logic.md`.

## Today's task — changed scope

Original ask: remove the "PO line status" filter from the COMS report. **Cancelled by user.**
Replaced with: mark the two orphaned model objects (`_po_line_status` column, `PO line status PO view
(FX)` measure) with a `[DEL]` prefix. Plan section A on the board.

Worth recording why the original ask dissolved: the slicer titled "PO line status" is bound to
`_PO_VIEW_[_status]` (lifecycle status), not to `_po_line_status`. The name and the binding disagree,
so "the PO line status filter" and "the PO line status column" are two unrelated objects.

## Blocked / open

- Report folders renamed to `__AVS/` / `__COMS/` mid-session — every documented path is stale until
  plan section B lands.
- Uncommitted Desktop edits sitting in COMS (active page + sort + sub-pixel nudges) need separating
  from deliberate work.
- Carried from day 1: `COMS DUMMY` SLA files, AVS `AUTO` vs `MAN`, scratch pages, no house theme, no
  per-table schema docs, ops unknowns.

## Next

Execute plan sections A → B → C, validate, then hand over the commit block.

## Closed

Written 2026-08-19, eight days late, from the [2026-08-12](2026-08-12.md) note and `git log` — not
from recall. What the `Next` above actually became:

- **Section A ran, and it is what broke the report.** The `[DEL]` rename was applied to
  `_PO_VIEW_.tmdl`, `cultures/en-US.tmdl` and `__FX__.tmdl`. The rename logic was correct in all
  three files, but the in-place rewrite of `__FX__.tmdl` prepended a UTF-8 BOM and Power BI Desktop
  refused to open the project the next morning. That incident is what produced **Rule #1** in
  `CLAUDE.md`.
- **Section A was then undone.** The user dropped in a clean report copy on 08-12, which reverted
  the rename along with the BOM. Board section A is open again.
- **Sections B and C** — no evidence either way in the 08-12 note. The `__AVS/` / `__COMS/` rename
  did land eventually (commits `6b64274`, `8b3ee60`), but not on this day's plan.

No commit block was handed over on 08-11; the day's first commit is `2861ff5`, dated 08-12.

