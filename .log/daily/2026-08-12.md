# Daily — 2026-08-12

**Day 3** — derived: origin 2026-08-10 (first commit `9ac9f79`); (2026-08-12 − 2026-08-10) + 1 = 3.
**Reports onboarded: 2** — derived by counting active rows in the CLAUDE.md registry.

_Closed retroactively on 2026-08-19 — see [Closed](#closed) at the end._

## Shipped

**1. COMS report was dead on open — diagnosed to 3 bytes.**
Desktop refused the project: `__FX__.tmdl` carried a UTF-8 BOM. Yesterday's `[DEL]` rename had
rewritten the file in place and prepended `EF BB BF`. Diffed against `HEAD`, the rename itself was
correct in all three files it touched (`_PO_VIEW_.tmdl`, `cultures/en-US.tmdl`, `__FX__.tmdl`,
including the DAX re-escaping `'_PO_VIEW_'[[DEL]] _po_line_status]`). A BOM scan across every
`.tmdl`/`.json`/`.pbir`/`.pbip` in `__COMS/` and `__AVS/` returned exactly one hit. Nothing else was
damaged — the report died on encoding, not on logic.

**2. Report replaced by user; `[DEL]` work rolled back with it.**
Clean copy dropped in, whole tree re-stamped 11:24, BOM scan now zero hits. The replacement also
reverted the 2026-08-11 rename — `_PO_VIEW_` is back to `column _po_line_status`, `__FX__` to
`measure 'PO line status PO view (FX)'`, culture bindings original. **Board section A is undone and
open again.**

**3. Rule #1 added to `CLAUDE.md` — no edits without consent.**
New top section, marked as outranking every rule below it: consent is explicit and per-task (a
question is never an edit request); default action is to *suggest* a diff, not apply it; copy every
touched file before the first write; a rewrite must reproduce the target's encoding, BOM state and
line endings byte-for-byte. Pointer line under *Core coding rules* (existing 1–5 not renumbered),
`__backup__/` added to `.gitignore` and to both CLAUDE.md blocks describing it.

**4. `_rdd_eta` reviewed and EK aligned to PO.**
EK guarded on `_del` but computed from `_full_eta` — **the guard tested a column the formula never
used**, so every in-transit line returned null while the guard protected nothing in the other
direction. PO used `coalesce(_arrival_date_full,_full_eta)`. Same column name, two different metrics.
EK `COMS EK VIEW.sql:1081-1083` aligned to PO; PO untouched.

**5. Three more columns reviewed, two aligned.**
`_crd_2_etd` (EK:1087-1089) and `_nbd_2_eta` (EK:1142-1143) aligned to PO. Note `_crd_2_etd`'s old
EK logic was **already equivalent to `_full_etd - _crd` in both branches** — when
`_revised_etd = coalesce(etd_wakeo,etd)` is non-null, `_full_etd` equals it — so the real change is
*prefer actual departure*. `_nbd_2_eta`'s status guard was already byte-identical to PO.

**6. `_etd_2_eta` / `_eta_2_del` unblocked, then five more columns reviewed.**
User moved both columns by hand into EK's `calc` CTE (907-916) and commented out the old `pre_calc`
copies (447-459, every line, with 460 still opening on a comma so the select list stays valid) —
verified normalized-identical to PO's `_calc`. Of the next five columns, only **one** needed work:
`_iss_cont_booking_perf` (EK 1052-1058), where EK used bare `y._full_etd` and PO uses
`coalesce(coalesce(_departure_date_actual,_departure_date), y._full_etd)` in all three spots. Note PO
writes that nested coalesce **inline rather than using `_departure_date_fallback`** — the alias is
defined later in the same select list (EK:1109 / PO:1063), the sibling-alias rule again; EK now
mirrors PO exactly instead of "improving" it. `_iss_transit_lead_time_perf`, `_iss_custom_clear_perf`,
`_ptd_discrepancy_days` and `_pta_discrepancy_days` were **already identical** to PO — confirmed by
whitespace-normalized diff, not by eye. `_dep_discrepancy_days` / `_arr_discrepancy_days` **skipped by
user decision**: they do not exist in EK under any name, so adding them would have been a shape change
(3 sites each + 2 new columns on `_EK_VIEW_`), not a logic alignment.

All SQL edits followed Rule #1: pre-edit copies in `context/datasources/sql/__backup__/` (verified
byte-identical, confirmed gitignored), post-edit CR/LF counts unchanged, sizes reconciled to the
exact character delta, no BOM, `validate.py` green each time. PO VIEW never in the diff.

**Commits today**: `2861ff5` (11:14) — Rule #1 + `.gitignore`; `f512d79` (17:39) — all six changed
columns in `COMS EK VIEW.sql` + the coms log + this note. All SQL work is committed; only the later
edits to this note are outstanding.

## Blocked / open

- ~~**`_etd_2_eta` and `_eta_2_del` cannot be aligned as-is**~~ — **resolved 2026-08-12** by moving
  both into `calc` by hand. The blocker was real: in `pre_calc` they sat in the same select list that
  defines `_arrival_date_full` (316) and `_departure_date_full` (331), and PostgreSQL forbids
  referencing a sibling select-list alias — the file proves it by re-pasting the whole 6-line
  `status_updates` subquery rather than referencing the alias one line above. **The stage move was the
  right fix, not a third copy of the subquery.** Worth remembering as the pattern for this file:
  a column needing `_*_full` aliases belongs in `calc`, not `pre_calc`.
- **`_rdd_eta` duplicates `_nbd_2_eta`** in both files — identical arithmetic, differing only in
  that `_nbd_2_eta` excludes Cancelled/Pending/Not Due. So cancelled lines still emit a delay figure
  via `_rdd_eta`. `_avg_lt` already builds on `_nbd_2_eta`. Retiring one is an open call.
- **The `case` wrapper is a no-op** in `_rdd_eta` and `_crd_2_etd` in *both* files — guard expression
  equals compute expression. Replicated as-is to keep EK and PO diffable. Cosmetic, not urgent.
- **`COMS EK VIEW.sql:19` — `$sql$` → `--$sql$`, not mine, now committed.** Opening dollar-quote
  delimiter commented out while the closing `$sql$;` (now line 1605) is not, so the wrapper is
  asymmetric. It was absent from the 12:41 backup and present in the 16:23 one; it rode along into
  `f512d79` and is now in history. Looks like a deliberate run-the-body-directly toggle — but as it
  stands the script no longer assigns to `dev.ek_view` as line 18 intends. **Worth confirming.**
- **SQL edits do not reach the report.** These views live in `public.sql_source`; all six changed
  columns (`_rdd_eta`, `_crd_2_etd`, `_nbd_2_eta`, `_etd_2_eta`, `_eta_2_del`,
  `_iss_cont_booking_perf`) must be applied there to take effect.
- **`validate.py` is blind to BOMs** — it reads everything with `utf-8-sig` (lines 62, 73, 86), which
  strips the BOM before checking. It reported GREEN on the file Desktop refused. Reproducible;
  ~10-line fix, not done.
- **Board section A undone** by the report replacement — the `[DEL]` soft-delete task is open again.
- Carried: `__AVS/` / `__COMS/` rename still unstaged (git sees ~600 deletions + 2 untracked
  folders); CLAUDE.md registry paths still say `AVS/` / `COMS/`; uncommitted Desktop edits in COMS;
  no house theme; no per-table schema docs; ops unknowns.

## Next

Apply the six changed columns to `public.sql_source` so the report actually sees them, and settle the
`--$sql$` toggle at `COMS EK VIEW.sql:19` before that run. Then decide whether `_rdd_eta` or
`_nbd_2_eta` is retired.

## Closed

Written 2026-08-19, seven days late, from the [2026-08-14](2026-08-14.md) note and `git log` — not
from recall. **None of the three `Next` items were done**, and all three are still carried today:

- **Six columns → `public.sql_source`** — not applied. Still listed as open on 08-14 and on 08-19.
  The `f512d79` edits remain repo-only; the report has not seen them.
- **`--$sql$` toggle at `COMS EK VIEW.sql:19`** — not settled. Still asymmetric, still in history.
- **`_rdd_eta` vs `_nbd_2_eta`** — not decided. Still duplicated in both files.

What did happen after this day: 08-13 was dull, 08-14 delivered the port lookup inventory (a
read-only task that touched none of the above), and 08-15 → 08-18 were four more dull days. The last
commit on the repo is still `0e39fb8`.

The day's own work — the BOM diagnosis, Rule #1, and the six EK column alignments — was complete and
committed on the day (`2861ff5`, `f512d79`); only the follow-through stalled.

