# Daily — 2026-08-10

**Day 1** — derived: origin 2026-08-10 (first commit `9ac9f79`); (2026-08-10 − 2026-08-10) + 1 = 1.
**Reports onboarded: 2** — derived by counting active rows in the CLAUDE.md registry.

## Shipped

- `.log/` tree initialised: `coms/`, `daily/`, `plan/` + today's three files.
- `.gitignore` — `*.pbix` and `**/cache.abf` added; 44 MB of `.pbix` backups now protected from an accidental commit.
- `_scripts/validate.py` — JSON parse + encoding/mojibake + TMDL tab checks. Green on the repo, and negative-tested (catches broken JSON and space-indented TMDL).
- `context/` scaffolded: `business-logic.md`, `reports/avs.md`, `reports/coms.md`, `datasources/sources.md`, plus placeholders in `datasources/sql/` and `domains/`.
- `CLAUDE.md` corrected in 7 places — it now describes the repo that actually exists.
- Both reports audited for accessibility: PBIR v4.0 + TMDL, editable as code, no legacy format.

## Reports touched

None. No file under `AVS/` or `COMS/` was modified today — audit only.

## Blocked / open threads

- **Report SQL lives outside the repo.** Both models pull query text from `public.sql_source` (Postgres) keyed by `_report`/`_page`. A SQL change there produces no git diff. Decide: mirror into `context/datasources/sql/`, or accept the DB as source of truth for query text.
- **Unknown ops facts**: refresh schedule, gateway, credentials owner, workspace. All `TODO` in `context/datasources/sources.md`.
- **No house theme.** AVS sits on `CY24SU10`, COMS on `CY25SU11` — two different Microsoft base themes. `_templates/` doesn't exist yet.
- **Business truth is empty.** No KPI definitions, no confirmed grain for `__MAIN__` / `__CONTAINER__` / `__SKU__`, AVS `AUTO` vs `MAN` role semantics unknown.
- **Possible dead pages**: AVS `Cal test` (0 visuals) and `Page 1` (1 visual); COMS `EK check` — QA page or leftover?

## Commits suggested

One: `chore: bootstrap repo — logs, gitignore, validator, context scaffold, CLAUDE.md fixes`.

## Tomorrow's first task

Answer the three cheapest open questions — AUTO vs MAN, EK/PO meaning, and the fate of the scratch
pages — then decide the `public.sql_source` mirroring question, since everything about
`context/datasources/sql/` depends on it.

---

# EOD close — 2026-08-10

Written 2026-08-11. Day 1 of the ISS-GF stack; origin 2026-08-10, first commit `9ac9f79`.

## Shipped (full day)

**Repo bootstrap**
- `.log/` tree created (coms / daily / plan) and today's three files written.
- `.gitignore`: `*.pbix` added, then `.pbi` policy iterated twice — folder-wide, then reversed to
  **`cache.abf` only** on user's call. All `.pbi/*.json` are tracked; 22 MB of model cache and 44 MB
  of `.pbix` stay out of GitHub.
- `_scripts/validate.py` written: JSON parse + UTF-8/mojibake + TMDL tab checks. Negative-tested.
  Later widened to cover `.pbi/*.json`. Final baseline: 277 JSON / 325 text / 30 TMDL, green.
- `CLAUDE.md` corrected in 8 places — it now describes the repo that exists, not a template.

**Reports audited**
- Both PBIR v4.0 + TMDL, no legacy format, fully editable as code.
- AVS: 6 pages / 111 visuals / 16 bookmarks / 10 tables, RLS via `CUSTOMDATA()`.
- COMS: 3 pages / 109 visuals / 4 bookmarks / 5 tables, no RLS.

**Business truth captured**
- 34 `.sql` files landed in `context/datasources/sql/` — the `sql_source` mirroring question is
  answered by fact. `README.md` indexes all of them by `_report`/`_page` with the deploy pattern.
- Read both COMS queries end to end (2071 + 1607 lines) and wrote
  `context/domains/shipment-tracking.md`: entity hierarchy, quantity flow, exact ETD/ETA/PTD/PTA/
  ATD/ATA definitions and fallback chains, EDD, the 5-bucket RAG exception framework with
  SLA-from-`sla_master_coms`, lifecycle statuses, lead-time/health-check incl. the 500-day guard.
- `context/reports/coms.md` rewritten; `business-logic.md`, `sources.md`, `avs.md` updated.
- Three findings raised and **all three confirmed by user as correct-by-design**: EK VIEW's
  freight-order grain, the `purchase_order_on_freight_unit → PurchaseOrderLine.id` key, and the
  `_crd_actual` array-order asymmetry.

## Reports touched

**None.** No file under the report or model definitions was modified on day 1 — audit and
documentation only.

## Commits

- `9ac9f79` first commit (pre-existing)
- `af7b101` bootstrap — logs, gitignore, validator, context scaffold, CLAUDE.md fixes
- `7bc40e4` ignore `.pbi` folders wholesale, untrack committed session state
- `8d833df` track `.pbi` session-state json, ignore only `cache.abf`
- `c6f75ef` COMS business logic and shipment-tracking domain from SQL source

## Correction logged

`__FX__` was documented as "FX rates / currency conversion" — **wrong**, inferred from the name. It
is a measures table of `FIRSTNONBLANK()` column wrappers (`FX` = fix). Corrected 2026-08-11 in
`reports/coms.md` and `business-logic.md`. Exactly the failure mode CLAUDE.md rule 2 warns about.

## Open threads at close

`COMS DUMMY` SLA files · AVS `AUTO` vs `MAN` semantics · scratch pages (AVS `Cal test`, `Page 1`,
COMS `EK check`) · no house theme · no per-table schema docs · ops unknowns (refresh, gateway,
credentials owner, workspace).
