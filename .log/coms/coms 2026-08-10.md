# Coms — 2026-08-10

- **09: session open — init `.log/`, init coms/daily/plan files.** Created `.log/coms/`, `.log/daily/`, `.log/plan/` with today's files and an active board. Origin date for all derived counters set to **2026-08-10** (first commit `9ac9f79`). #decision
- **09: check skills.** Two skills installed locally: `pbip-editor` and `dax-sql-formatter`. CLAUDE.md claimed pbip-editor was the ONLY one — false, corrected. #bug
- **09: check reports — accessible?** Yes, both. `AVS/Tracking customer version` and `COMS/Coms report` are PBIR v4.0 enhanced format + TMDL, no legacy `report.json` anywhere, 267 JSON files all parse, 30 TMDL files all tab-indented. Fully editable as code.
- **09: are AVS/COMS domains?** No — user clarified they are **report projects** within one client system, and the set will grow. Repo layout stays `<CODE>/` at root; CLAUDE.md rewritten to match instead of moving files. Report-project docs go to `context/reports/<code>.md`, leaving `context/domains/` for genuine cross-report domains. #decision
- **09: add all `*.pbix` to gitignore.** Done — `.gitignore` now carries `*.pbix` and `**/cache.abf` alongside the two `.pbi/` rules. 44 MB of backups in `AVS/__backup__/` + `COMS/__backup__/` are now unignorable by accident; none were tracked, so no `git rm --cached` needed.
- **09: build `_scripts/validate.py`?** Yes — built. Three checks (JSON parse, UTF-8/mojibake encoding, TMDL tab indent), exits non-zero naming the offending file. Baseline green: 267 JSON / 309 text / 30 TMDL, 0 problems. Negative-tested against a deliberately broken JSON and a space-indented TMDL — both caught. #decision
- **09: scaffold `context/`?** Stubs only, filled with what the models prove, `TODO` where I'd be guessing. Notable find: report SQL lives in a DB table `public.sql_source` keyed by `_report`/`_page`, so it is **outside** this repo and invisible to git diff. #idea — decide whether to mirror it into `context/datasources/sql/`.
- **09: check CLAUDE.md + nano summary of rules.** CLAUDE.md corrected in 7 places (client name, structure tree, stack, backups path, skills, registry, naming). Summary delivered in chat.
- **10: does CLAUDE.md have a rule ignoring all `.pbi` subfolders?** **No** — it had two file-level rules (`.pbi/localSettings.json`, `.pbi/cache.abf`), never a folder rule. Rewritten to `**/.pbi/` in both `.gitignore` and CLAUDE.md, with the reason recorded: `cache.abf` is a full local copy of the model data (21 MB AVS), which must never reach a cloud remote. #decision
- **10: reversed — track all `.pbi/*.json`, ignore only `cache.abf`.** User's call: the model data cache is the only thing that cannot be published; the session-state JSONs go back into git. `.gitignore` and CLAUDE.md updated to `**/.pbi/cache.abf`. 9 JSON files return to tracking. Flagged and accepted: `localSettings.json` carries a `securityBindingsSignature` (DPAPI blob, bound to the local Windows user) that will churn a one-line diff whenever a different machine opens the project. #decision
- **11: add COMS business logic + domain.** SQL library landed in `context/datasources/sql/` (34 files) — thread #1 resolved by fact: the mirror exists. Read both COMS queries end to end (2071 + 1607 lines) and wrote `context/domains/shipment-tracking.md` (entity hierarchy, quantity flow, exact ETD/ETA/PTD/PTA/ATD/ATA definitions and fallback chains, EDD, 5-bucket RAG exception framework + SLA-from-`sla_master_coms`, lifecycle statuses, lead-time/health-check with the 500-day guard, client scoping via `iss_domain`). Rewrote `context/reports/coms.md`, updated `business-logic.md`, `sources.md`, `avs.md`, and added `sql/README.md` with the full inventory + deploy pattern. #decision
- **11: grain finding — EK VIEW groups by freight order, not purchase order.** The SQL does `group by _fo_id` (sha256 of `freight_unit.id`), ranks POs by `current_po_promised_dt`, takes `_sort = 1` as primary PO and string-aggregates the rest into `_secondary_po`. Raised for confirmation.
- **11: join relationship documented.** `purchase_order_on_freight_unit.purchase_order_id` points at `PurchaseOrderLine.id`, not a PO header; `po_no` is not unique in `PurchaseOrderLine`. Raised for confirmation.
- **12: all three findings confirmed by user.** (a) EK VIEW grain is **as designed** — grouped on the currently active/main freight order, other linked orders aggregated into secondary-order columns; (b) the `purchase_order_on_freight_unit → PurchaseOrderLine.id` key is **correct, leave as is**; (c) the `_crd_actual` array-order asymmetry is **deliberate**, left as a hook for further amendments — do not "fix". Docs updated: all three now read as settled facts, board threads 8/10/11 closed. #decision
- **10: fallout — 5 `.pbi` files were already committed and pushed** in `af7b101` to `github.com/rusloc/ISS_PBI` before the folder rule existed: `editorSettings.json` ×2, `tmdlScripts.json` ×2, `daxQueries.json` ×1. Contents inspected — editor toggles and tab order only. **No `.pbix`, no `cache.abf`, no `localSettings.json` in any commit** (verified across all history), so no model data left the machine. Fix is `git rm -r --cached` + commit; no history rewrite needed. #bug
