# Coms — 2026-08-20

- **Write yesterday (2026-08-19) as dull.** Started to, then stopped: `git log` contradicts it. **`d20fcc3` was committed on 08-19 at 16:40** — *"change ARRIVAL / DEPARTURE date logic as per Mikhail request"* — carrying 13 report files and ~900 lines (PO VIEW 680, EK VIEW 187, `en-US.tmdl` 268, both `_*_VIEW_` tables, both param tables, 3 visuals). 08-19 was not a dull day. #bug
- **08-19 written as a real day instead.** Recorded `d20fcc3` as the day's delivery, with the caveat that the edits were already uncommitted in the working tree beforehand — the authoring predates 08-19, the commit does not. Log housekeeping kept as item 2. The 08-19 plan and coms files left untouched; both were accurate. #decision
- **Three findings raised on `d20fcc3`, none acted on.** It is a mixed commit — 16 `.log/` files and `PO EK view column logic align.txt` ride along under a message naming only the date-logic change, against the one-theme-per-commit rule. The change itself is undocumented: no plan, no coms, no note of what Mikhail asked for. And `COMS EK VIEW.sql` moved 187 lines in it, so whether it overlaps the six 08-12 columns still pending for `public.sql_source` is unverified. #bug
