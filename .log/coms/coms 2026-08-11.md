# Coms — 2026-08-11

- **11:5x: log yesterday, plan today — task: remove the "PO line status" filter from the COMS report.** Closed the 2026-08-10 daily with a full EOD, opened today's files, drafted the task plan on the board.
- **11:5x: repo changed under me — report project folders renamed.** `AVS/` → `__AVS/` and `COMS/` → `__COMS/` (the COMS rename landed mid-investigation). 172 files each; git will record them as renames once staged. Every path reference in `CLAUDE.md`, `context/reports/*.md` and `context/datasources/sql/README.md` is now stale — folded into today's plan. #bug
- **11:5x: uncommitted Desktop edits found in COMS.** Two visuals **deleted** from the PO view page — a textbox (`41d9fad5…`) and a dropdown slicer titled "Status" bound to `_PO_VIEW_[_status]` (`5102d740…`); report went 109 → 107 visuals. Plus cosmetic changes: `pages.json` active page → EK view, a `sortDefinition` on `_po_no_ekporef`, sub-pixel nudges on two EK view visuals. None of it committed. #bug
- **11:5x: two `_status` slicers existed on PO view** — "Status" (now deleted in Desktop) and "PO line status" (`541655e7…`, still present). Both bound to the same column. Flagged to confirm the survivor is the intended one.
- **11:5x: target located.** "PO line status" is a **dropdown slicer** (`541655e745ba6a74c0b2`) at y=362 in the `Slicers divs` group (`1e6a9523ddc670be2170`) on the **PO view** page. No bookmark references it — clean to delete. #decision
- **11:5x: naming mismatch found.** That slicer is titled "PO line status" but is bound to `_PO_VIEW_[_status]` — the **lifecycle status** (Delivered / In transit / Booked / …), not the `_po_line_status` column. The real `_po_line_status` feeds the `PO line status PO view (FX)` measure in `__FX__`, which is a different object and is **not** in scope for removal. #bug
- **11:5x: task cancelled and replaced.** User cancelled the slicer removal. New task: rename the two orphaned objects with a `[DEL]` prefix (`_po_line_status` column in `_PO_VIEW_`, `PO line status PO view (FX)` measure in `__FX__`) — soft-delete marker for a later cleanup pass. The slicer stays. #decision
- **11:5x: `__CODE__` folder convention adopted.** `__AVS/` / `__COMS/` is the new standard; CLAUDE.md naming rule, registry and every path reference in `context/` to be updated in the same commit. #decision
- **11:5x: correction to yesterday's docs.** `__FX__` was documented as "FX rates / currency conversion" — wrong, inferred from the name. It is a measures table of `FIRSTNONBLANK()` column wrappers (`FX` = fix), 7 measures + a `Placeholder` column. Fixed in `reports/coms.md` and `business-logic.md`. #bug
