# Coms — 2026-08-12

- **COMS report will not open — root cause: a 3-byte UTF-8 BOM.** Desktop: `Cannot read '…\__COMS\Coms report.SemanticModel\definition\tables\__FX__.tmdl'. Only text with UTF8 encoding without BOM is supported.` Yesterday's `[DEL]` rename rewrote `__FX__.tmdl` in place and prepended `EF BB BF`. Diffed vs `HEAD`, the rename itself is **correct in all three files** — `_PO_VIEW_.tmdl` (column), `cultures/en-US.tmdl` (2 linguistic bindings) and `__FX__.tmdl` (measure + DAX re-escaped to `'_PO_VIEW_'[[DEL]] _po_line_status]`). BOM scan over every `.tmdl`/`.json`/`.pbir`/`.pbip` in `__COMS/` and `__AVS/`: exactly one hit. Nothing else damaged. #bug
- **`validate.py` reported green on the broken file.** It reads with `utf-8-sig` (lines 62, 73, 86), which *strips* the BOM before every check — so validator-green is currently not evidence a TMDL file will open. Hardening deferred, not done. #bug
- **User restores the report from their own copy** — no repair attempted in the repo. #decision
- **COMS report replaced fully.** User dropped in a clean copy — whole `__COMS/` tree re-stamped 11:24. Verified: BOM scan over every `.tmdl`/`.json`/`.pbir`/`.pbip` returns **zero hits**, report opens again. Note the replacement also rolled back **yesterday's `[DEL]` rename** — `_PO_VIEW_` is back to `column _po_line_status`, `__FX__` to `measure 'PO line status PO view (FX)'`, and `cultures/en-US.tmdl` to the original bindings (0 `[DEL]` hits anywhere). The soft-delete task from 2026-08-11 is therefore **undone and still open**. #decision
- **`_rdd_eta` logic reviewed across both COMS SQL sources.** EK and PO define the same column differently. EK (`COMS EK VIEW.sql:1080`) guarded on `_del` (delivery date) but computed from `_full_eta` — **the guard tested a column the formula never used**, so every in-transit line (ETA known, not yet delivered) returned null, and the guard protected nothing in the other direction (`null - date` is already null). PO (`COMS PO VIEW.sql:1265`) used `coalesce(_arrival_date_full,_full_eta)` — actual arrival once known, else ETA, per the `_*_full` rule in `domains/shipment-tracking.md:92`. Result: same column name, two different metrics; the same PO line yielded different values in the two views once ATA landed. #bug
- **EK aligned to PO — PO is the true source, PO file untouched.** `COMS EK VIEW.sql:1081-1083` now matches PO's expression exactly. Rule #1 followed: pre-edit copy to `context/datasources/sql/__backup__/` (verified byte-identical, confirmed gitignored) before the write; post-edit CR/LF counts unchanged (1607/1607), size +62 = exact character delta, no BOM, `validate.py` green. #decision
- **Two `_rdd_eta` findings left open by design.** (a) The `case` is a **no-op in both files** — guard expression equals compute expression, so it reduces to the bare subtraction; replicated as-is to keep EK and PO diffable. (b) `_rdd_eta` duplicates `_nbd_2_eta` (EK:1139 / PO:1321) — identical arithmetic, the only difference being that `_nbd_2_eta` excludes Cancelled/Pending/Not Due while `_rdd_eta` does not, so **cancelled lines still emit a delay figure**. `_avg_lt` already builds on `_nbd_2_eta`, not `_rdd_eta`. Retiring one of the two is a separate call. #idea
- **Four more columns reviewed for EK→PO alignment; 2 of 4 aligned, 2 blocked.** Aligned in `COMS EK VIEW.sql`: `_crd_2_etd` (1087-1089, CTE `calc`) — dropped the `_revised_etd` branch for PO's `coalesce(_departure_date_full, _full_etd)` and `else null`; note the old EK logic was **already equivalent to `_full_etd - _crd` in both branches** (when `_revised_etd = coalesce(etd_wakeo,etd)` is non-null, `_full_etd` equals it), so the real behavioural change is *prefer actual departure*. And `_nbd_2_eta` (1142-1143, CTE `main`) — `z._full_eta` → `(coalesce(_arrival_date_full,_full_eta))`; status guard was already byte-identical to PO. #decision
- **`_etd_2_eta` and `_eta_2_del` NOT aligned — hard PostgreSQL blocker.** Both live in EK's `pre_calc` select list (447-454 / 455-459), the *same* select list that defines `_arrival_date_full` (316) and `_departure_date_full` (331); PostgreSQL forbids referencing a sibling select-list alias. The file proves it: `_arrival_date_full` does not reference `_arrival_date_actual` one line above, it **re-pastes the whole 6-line `status_updates` subquery**. PO computes both in `_calc`, one stage later, where the aliases exist. Aligning would mean a third copy of that subquery or moving the columns to a later CTE — duplication or restructuring, both out of scope. Left untouched pending a decision. #bug
- **Unrelated uncommitted change found in `COMS EK VIEW.sql:19`, not mine.** `$sql$` → `--$sql$` (opening dollar-quote delimiter commented out) while the closing `$sql$;` at 1595 is untouched — the wrapper is asymmetric. Absent from HEAD and from the 12:41 backup, present in the 16:23 backup, so it landed between those two. Looks like a deliberate "run the body directly" toggle. Left alone; it will ride along in any commit of this file. #bug
- **`_etd_2_eta` / `_eta_2_del` unblocked by user** — both moved by hand into EK's `calc` CTE (907-916), old `pre_calc` versions fully commented out (447-459, line 460 still opens with a comma so the select list stays valid). Verified whitespace-normalized **identical to PO** `_calc` (1052-1061). The sibling-alias blocker is resolved: in `calc` the columns read `_arrival_date_full` / `_departure_date_full` from `pre_calc` via `y.*`. #decision
- **Five more columns reviewed; 1 aligned, 2 already identical, 2 skipped.** Aligned: `_iss_cont_booking_perf` (EK 1052-1058) — EK used bare `y._full_etd`, PO uses `coalesce(coalesce(_departure_date_actual,_departure_date), y._full_etd)` in all three spots (guard, `<= 7` test, and the divisor expression). Same *prefer actual departure* pattern as `_crd_2_etd`. Note **PO writes that nested coalesce inline rather than using `_departure_date_fallback`** because the alias is defined later in the same select list (EK:1109 / PO:1063) — sibling-alias rule again; EK now mirrors it exactly. Verified normalized-identical to PO. #decision
- **`_iss_transit_lead_time_perf` and `_iss_custom_clear_perf` needed no change** — already whitespace-normalized identical to PO (EK 1069-1080 / 1081-1089 vs PO 1244-1255 / 1256-1264). Same for `_ptd_discrepancy_days` and `_pta_discrepancy_days` (EK 1156-1167 vs PO 1495-1500 / 1507-1512). #decision
- **`_dep_discrepancy_days` / `_arr_discrepancy_days` — skipped by user decision.** They do **not exist in EK under any name**; PO has four discrepancy columns, EK only two. They are the "vs full estimate" twins (`_departure_date_fallback - _full_etd`, `_arrival_date_fallback - _full_eta`) of the "vs planned PTD/PTA" pair EK already carries. Adding them was not a logic alignment but a shape change — 3 insertion sites each (`main` after 1167, aggregation after 1288, union null-pad after 1479) plus 2 new columns on `_EK_VIEW_` in the semantic model. Checked and ruled out that they exist under other names: `_days_delayed_eta`/`_days_delayed_etd` are present in *both* files and are distinct. #decision
- **Repo edit does not reach the report.** These views live in `public.sql_source` on the DB; the change must be applied there to take effect. #decision
- **Rule #1 added to `CLAUDE.md`: no edits without consent.** New top section, marked as outranking everything below it: consent is explicit and per-task (a question is never an edit request); default action is to *suggest* a diff, not apply it; copy every touched file to `<CODE>/__backup__/` before the first write; a rewrite must reproduce the target's encoding, BOM state and line endings byte-for-byte. Pointer line added under *Core coding rules*, `__backup__/` added to `.gitignore` and to the structure/gitignore blocks. #decision
